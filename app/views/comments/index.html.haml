%ol.comments.comments1
  - @comments.each do |comment|
    %li
      = render "comments/comment", :comment => comment, |
        :show_story => true                             |
      %ol.comments
.morelink
  - if @page && @page > 1
    %a{:href => "/comments#{@page == 2 ? "" : "/page/#{@page - 1}"}"}
      \&lt;&lt;
      Page #{@page - 1}
  - if @comments.any?
    - if @page && @page > 1
      |
    %a{:href => "/comments/page/#{@page + 1}"}
      Page
      \#{@page + 1} &gt;&gt;
